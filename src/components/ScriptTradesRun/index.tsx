import React from 'react';

import DistributionConfig from './DistributionConfig';
import Tasks from './Tasks';

const ScriptTradesRun = () => {
	return (
		<div>
			<DistributionConfig />
			<Tasks />
		</div>
	);
};

export default ScriptTradesRun;